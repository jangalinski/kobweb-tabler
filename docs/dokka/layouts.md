# Package com.github.jangalinski.kobweb.tabler.layouts

Kobweb layouts that render shared Tabler page chrome around route content.

Pages usually reference these layouts through Kobweb `@Layout` annotations and provide route-specific data through `@InitRoute`.
