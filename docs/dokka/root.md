# Package com.github.jangalinski.kobweb.tabler

Root package for library-level marker objects, shared value contracts, icon-name constants, and Kobweb initialization hooks.

Most application code imports from the narrower `components`, `models`, `layouts`, `elements`, or `styles` packages. Use this package when integrating root-level library setup or shared primitive value types.
