# Package com.github.jangalinski.kobweb.tabler.components

Reusable Tabler components implemented as Compose HTML and Kobweb composables.

This package contains the main visual building blocks, including cards, tables, table cards, avatars, breadcrumbs, headers, footers, navigation, stat cards, and tracking displays. Prefer these components when a page needs a complete Tabler UI pattern.
