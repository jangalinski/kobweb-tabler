# Package com.github.jangalinski.kobweb.tabler.charts

Small chart wrappers that integrate JavaScript chart libraries with Kobweb pages while keeping the public API Kotlin-first.

Use this package for chart components whose rendering depends on browser-side JavaScript lifecycle hooks.
