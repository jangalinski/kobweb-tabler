# Package com.github.jangalinski.kobweb.tabler.models

Pure configuration and state models consumed by the Tabler components.

This package holds serializable-style data structures for navigation, breadcrumbs, images, avatars, tables, pagination, and layout metadata. Use these models when the UI should be driven from data rather than arbitrary composable lambdas.
