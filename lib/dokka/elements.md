# Package com.github.jangalinski.kobweb.tabler.elements

Contains small, reusable composables that do not exactly qualify as a full component, but are useful for building components or pages.

Use this package for narrow HTML helpers such as Tabler icon rendering.
