# Package com.github.jangalinski.kobweb.tabler._foundation.css

Shared CSS-class and modifier infrastructure used across Tabler concept renderers. These are implementation primitives, not a general consumer-facing class catalogue.
